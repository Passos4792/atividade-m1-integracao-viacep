$kind: http-request
url: "{{baseUrl}}/product/{{productId}}"
method: DELETE
scripts:
  - type: afterResponse
    code: pm.test('HTTP 204', function () { pm.response.to.have.status(204); });
    language: text/javascript
order: 27000
