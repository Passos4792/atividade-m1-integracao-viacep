$kind: http-request
name: "19 - Porto Alegre: disponível"
url: "{{baseUrl}}/product/{{productId}}/availability?cep=90010000"
method: GET
queryParams:
  cep: "90010000"
scripts:
  - type: afterResponse
    code: |-
      pm.test('HTTP 200', function () { pm.response.to.have.status(200); });
      pm.test("Disponibilidade true", function () { pm.expect(pm.response.json()).to.eql(true); });
    language: text/javascript
order: 19000
