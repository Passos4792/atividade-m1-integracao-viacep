$kind: http-request
url: "{{baseUrl}}/product/{{productId}}/availability?cep=08773-380"
method: GET
queryParams:
  cep: 08773-380
scripts:
  - type: afterResponse
    code: |-
      pm.test('HTTP 200', function () { pm.response.to.have.status(200); });
      pm.test("Disponibilidade true", function () { pm.expect(pm.response.json()).to.eql(true); });
    language: text/javascript
order: 10000
