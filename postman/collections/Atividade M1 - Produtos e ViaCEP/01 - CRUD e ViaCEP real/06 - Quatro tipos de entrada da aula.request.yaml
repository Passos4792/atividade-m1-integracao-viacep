$kind: http-request
url: "{{baseUrl}}/product/category/atividade-m1?categoryAsParam=atividade-m1"
method: GET
headers:
  categoryAsHeader: atividade-m1
  Content-Type: application/json
queryParams:
  categoryAsParam: atividade-m1
scripts:
  - type: afterResponse
    code: |-
      pm.test('HTTP 200', function () { pm.response.to.have.status(200); });
      pm.test("Header, path, param e body aceitos", function () { pm.expect(pm.response.json()).to.have.length.above(0); pm.expect(pm.response.json().every(p => p.category === 'atividade-m1')).to.eql(true); });
    language: text/javascript
order: 6000
